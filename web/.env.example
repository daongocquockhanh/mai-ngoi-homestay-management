# In development this variable is not needed — Vite proxies /api to
# http://localhost:3000 (see vite.config.ts).
#
# In production (Cloudflare Pages build), set this to the Render backend URL.
# Example:
# VITE_API_BASE_URL=https://mai-ngoi-api.onrender.com
VITE_API_BASE_URL=
